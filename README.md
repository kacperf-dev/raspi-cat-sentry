# raspi-cat-sentry
A custom computer vision system running on Raspberry Pi that uses a fine-tuned YOLOv8 (NCNN) model to identify a specific cat and send notifications only when the cat is waiting at the door.
